# Interim Data

Use this folder for temporary transformed datasets produced during analysis.

Examples:

- flattened feature tables
- sampled subsets for debugging
- cleaned intermediate parquet files
